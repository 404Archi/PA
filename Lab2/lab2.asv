% a, G, H, R [m]
% h [m]
% q [m^3/s]
% c [m^2]

R1 = 1.59577;
H1 = 17;
c1 = 4.12311;
a1 = pi * R1 .^ 2;

R2 = 2.98541;
H2 = 17;
c2 = 2.91548;

sim('lab2s');

% 1/3 *  pi * R/H * 3h^2(t) * h'(t) - c*sqrt(h(t))
% R2 / H2

% v = 1/3 * pi * r^2 * h

% R2/H2 * h2^2 * pi * 1/3 * h2'(t) = v2

% a2 = R2/H2 * h2^2 * pi;
% v2 = 1/3 * a2 * h2;
% v2 = 1/3 * R2/H2 * pi * 3*h2^2 * h2'(t);


